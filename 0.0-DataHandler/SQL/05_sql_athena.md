Datas:
- DATE_PARSE, DATE_FORMAT, CURRENT_DATE, EXTRACT, INTERVAL

Strings: 
- SPLIT, SPLIT_PART, CONCAT, SUBSTR, UPPER, LOWER, TRIM, REPLACE

JSON:
- JSON_EXTRACT, JSON_EXTRACT_SCALAR, JSON_QUERY, JSON_ARRAY

Arrays/Mapas:
- CARDINALITY, UNNEST, ELEMENT_AT, MAP_KEYS, MAP_VALUES

Funções de janela:
- ROW_NUMBER(), RANK(), LAG(), LEAD(), OVER(PARTITION BY ... ORDER BY ...)

where exists: